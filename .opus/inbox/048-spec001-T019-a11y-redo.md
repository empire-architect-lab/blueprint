# Task 048 — Spec 001 / T019 a11y (redo, expanded whitelist)

**From:** Cowork Opus
**To:** Code Agent
**Branch:** `feat/001-the-cursor`
**Decision:** Option (a) from 047-reply. Bundle both real bugs into the audit task.

## Action

Finish the axe integration on all 6 e2e specs AND fix the two real WCAG AA violations T019 surfaced, in one product commit.

## Whitelist (expanded)

- `e2e/**` (axe integration on all 6 specs)
- `src/components/cursor/skip-link.tsx` (contrast fix)
- `src/components/cursor/cinematic-intro-mobile.tsx` (dispatch `blueprint:cinematic-complete` when its `hero` beat first renders, so router unmounts the SkipLink overlay on mobile)
- `specs/001-the-cursor/tasks.md` (tick T019 + the T018 "all 5 green locally" sweep line)
- `.logs/048-T019.log`, `.logs/001-a11y-walkthrough.log`
- `.opus/inbox/048-*.md`, `.opus/outbox/048-reply.md`

Source is otherwise read-only. Do not touch `cinematic-router.tsx` — fix at the mobile component, not by gating the router.

## Acceptance

- All 6 specs run axe at final state with tags `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa` → 0 violations.
- Mobile spec confirms SkipLink is unmounted after mobile cinematic finishes.
- `.logs/001-a11y-walkthrough.log` documents the audit + the two fixes.

## Required scripts (must pass, paste tail in reply)

1. `tsc --noEmit`
2. `eslint` + `prettier`
3. `playwright` (all 6 specs)
4. `gitleaks`
5. `check-forbidden-terms` + `check-i18n`

Open PR (or push to existing PR #15). Do not mark done. Bookkeeping commit per CLAUDE.md.

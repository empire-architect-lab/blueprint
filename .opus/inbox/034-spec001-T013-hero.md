# Task 034 — Spec 001 / T013 — Hero component

**From:** Cowork Opus
**To:** Code Agent
**Date:** 2026-04-08
**Branch:** continue on `feat/001-the-cursor` (rolling draft PR #15)
**Spec:** `specs/001-the-cursor/tasks.md` → T013 (all 7 boxes)

## Action

Implement T013 exactly as written in tasks.md. Hero component the `cinematic-complete` event reveals.

## Whitelist (only these)

- `src/components/hero/hero.tsx` (new, server component)
- `src/components/hero/replay-button.tsx` (new, client — bottom-right, triggers a `blueprint:cinematic-replay` CustomEvent; actual replay wiring lands in T015)
- `messages/{en,fr,ar,nl}.json` — add `hero.headline` and `hero.metadata` keys (ICU interpolation: sha, specs, tasks, lies)
- `src/app/[locale]/earth-preview/page.tsx` — on `CINEMATIC_COMPLETE_EVENT`, render `<Hero />` below the canvas (temporary preview mount; homepage integration is T016)
- `tests/unit/hero.test.tsx` (new) — renders with a mocked `BUILD_METADATA` + stubbed `/api/head-commit` fetch, asserts h1 + metadata line + `data-spec-id="001-the-cursor"`
- `specs/001-the-cursor/tasks.md` — tick all T013 boxes

Do NOT touch T014+ files (no mobile, no router, no homepage root).

## Acceptance

- `h1` uses `font-display text-[220px] leading-none` with `hero.headline` i18n key
- `p` uses `font-mono text-[18px]` with `hero.metadata` interpolating sha/specs/tasks/lies from `BUILD_METADATA` + `/api/head-commit`
- `data-spec-id="001-the-cursor"` on root
- `<ReplayButton />` bottom-right, absolute positioned inside hero
- All 4 locales have both keys; `check-i18n.sh` green

## Scripts (all 9 must pass)

typecheck, lint, vitest, playwright, gitleaks, npm audit, check-rls, check-tenant-id, check-forbidden + check-i18n. Save log to `.logs/034-T013.log`.

## Bookkeeping

Same-branch commit: `.logs/034-T013.log`, `.opus/outbox/034-reply.md`, this inbox file. Conventional commit: `feat(hero): t013 hero component per spec 001`.

Reply in `.opus/outbox/034-reply.md`. Then stand by for T014 dispatch.

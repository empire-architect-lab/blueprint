# 023 — Archive consumed tasks + neutralize parent CLAUDE.md

Two pieces of zero-risk housekeeping in one PR. Both are out-of-product-tree, both are about cleanliness.

## Part A — Archive consumed inbox/outbox to `.opus/archive/`

The live `.opus/inbox/` and `.opus/outbox/` directories have grown to 15+ and 22+ files respectively. Every task from 008 through 022 is fully consumed, replied to, and merged to main. They should not be sitting next to in-flight work. Move them to `.opus/archive/`.

### Use `git mv` to preserve history

For each task N from 008 through 022 inclusive, move its files into a per-task subfolder under `.opus/archive/`:

```
.opus/archive/008/
  inbox/008-wire-bot-and-approve.md
  outbox/008-reply.md
.opus/archive/009/
  inbox/009-fix-pr5-and-wire-bot.md
  outbox/009-reply.md
.opus/archive/010/
  inbox/010-approve-pr3-and-status.md
  outbox/010-reply.md
... (continue through 022)
```

**Specifics:**

- Use `git mv` (not `mv` + `git add`) so git tracks the rename and history follows the file.
- Per-task subfolders, **not** flat. Makes `git log .opus/archive/018/` show the entire 018 timeline (dispatch + reply + any logs) in one place.
- Inside each `archive/<N>/` folder, mirror the original layout: `inbox/` subdirectory and `outbox/` subdirectory. Don't flatten — keeps the dispatch/reply distinction visible.
- **Logs stay where they are** (`.logs/` is already organized by task ID and isn't bloating the working view). Only inbox/outbox move.

### What to move

Inbox (15 files):
- `008-wire-bot-and-approve.md`
- `009-fix-pr5-and-wire-bot.md`
- `010-approve-pr3-and-status.md`
- `011-bot-approve-pr2.md`
- `012-cleanup-branch-and-constitution.md`
- `013-constitution-fix-direction.md`
- `014-foundation-audit.md`
- `015-bot-approve-pr8.md`
- `016-housekeeping-techdebt-and-reaudit.md`
- `017-spec001-t001-with-sfx-env.md`
- `018-why-no-commits.md`
- `019-cleanup-and-execute.md`
- `020-close-pr11-and-pr10.md`
- `021-claude-md-rule-update.md`
- `022-execute-option-a.md`

Outbox (consumed replies):
- `002-question.md`, `002-reply.md` *(if still in outbox/ — the existing `.opus/archive/001-fix-ci-lockfile-and-checkboxes.md` suggests early tasks were partially archived already; verify what's actually in `outbox/` and only move what's there)*
- `003-question.md`, `003-reply.md`
- `004-reply.md`
- `007-reply.md` through `022-reply.md`

**Verify with `git ls-files .opus/outbox` first**, then move only the files that exist. Don't try to move files that aren't there.

### What stays in `.opus/inbox/` and `.opus/outbox/`

After this task, both directories should contain **only**:
- `.opus/inbox/023-housekeeping-archive-and-parent-claude.md` (this dispatch)
- `.opus/outbox/023-reply.md` (your reply)
- A `.gitkeep` in each directory if git would otherwise drop empty folders

The next task (T002) will land its own dispatch + reply in the live folders, and 023's files will get archived in 024 or whenever T002's bookkeeping rolls. From now on the live folders only ever hold one or two tasks at a time.

## Part B — Neutralize parent `../CLAUDE.md`

Now that `blueprint/CLAUDE.md` is canonical (merged in PR #12 as `d716d41`), the parent file at `c:\Users\AI Workspace\Projects\Blueprint Lab\CLAUDE.md` is a stale duplicate. Replace its entire contents with a one-line pointer:

```markdown
# CLAUDE.md has moved

The canonical CLAUDE.md for this project is now `blueprint/CLAUDE.md`, tracked in the git repository at https://github.com/empire-architect-lab/blueprint.

This parent file is intentionally left as a pointer so any tool that looks here finds its way to the real one. Do not edit this file. Edit `blueprint/CLAUDE.md` via the normal spec-kit dispatch flow.
```

**Important:** the parent file is **outside the git repo**, so it cannot be in the PR diff and cannot be CI-checked. You'll have to write it directly with a normal file write (no git involvement). Confirm in your reply that you wrote it and that the contents match the above.

If you cannot reach the parent path from your shell (sandboxing, permission, etc.), say so in the reply and I'll handle it from my side — do **not** improvise a workaround.

## Part C — One commit, one PR

- Branch: `chore/opus-archive-and-parent-pointer`
- Commit message: `chore(opus): archive tasks 008–022 and prepare for parent CLAUDE.md pointer`
- Files in commit:
  - All the `git mv` renames from Part A
  - **Nothing from Part B** — the parent file is outside the repo and doesn't appear in the diff
  - Bookkeeping for THIS task per Rule 1: `.opus/inbox/023-housekeeping-archive-and-parent-claude.md`, `.opus/outbox/023-reply.md`, `.logs/023-housekeeping.log`
- Run all 9 scripts, save log
- PR title: `chore(opus): archive consumed tasks 008–022`
- PR body: explain Part A (the archive structure), explain Part B (parent pointer, written separately because it's outside the repo)
- Bot-approve, squash-merge, delete branch
- Reply with merged commit OID, confirmation of Part B parent file write, and `tree .opus/inbox .opus/outbox` showing the now-near-empty live folders

## Rules

- `git mv` only. No `cp` + `rm` + `git add` shenanigans. We want git history to follow each file.
- One commit, one PR, squash-merge.
- Do **not** modify any product code.
- Do **not** modify the constitution.
- Do **not** modify `blueprint/CLAUDE.md` — Rule 1 + Rule 2 are already in main, this PR doesn't touch that file.
- Do **not** delete `.logs/` files. Only inbox/outbox move.
- If any file in the move list doesn't exist in `outbox/`, skip it silently and note which ones in the reply. Don't fail on missing files.

## Why bundle A and B

Both are zero-risk markdown housekeeping in the same conceptual area (cleaning up after the last 15 tasks). One PR is faster than two. The audit trail is preserved either way because each file move is its own git operation inside the squash commit.

After this lands, T002 dispatch goes out and we're back to normal cadence with a clean working view.

— Opus
